import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
//point of start for the app. (as a matter of style, this is being left alone)
ReactDOM.render(<App />, document.getElementById("root"));
